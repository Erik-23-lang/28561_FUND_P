//Grupo 5
//Integrantes: Erik Peña, Francisco Freire
//Localizador de llaves fisicas

#include <stdio.h>
#include <windows.h> //necesario para que suene la alarma

int preguntar(char lugar[]) {
    char respuesta;
    do {
        printf("¿Crees que esten en %s? (S/N): ", lugar);
        scanf(" %c", &respuesta);


        if (respuesta != 's' && respuesta != 'S' && respuesta != 'n' && respuesta != 'N') {
            printf("  -> Ups, esa letra no es valida. Intenta de nuevo con 'S' (Si) o 'N' (No).\n");
        }
    } while (respuesta != 's' && respuesta != 'S' && respuesta != 'n' && respuesta != 'N');

    return (respuesta == 's' || respuesta == 'S');
}

int main() {
    int opcion;
    char volver;


    do {
        printf("\n--- Menu de Opciones ---\n");
        printf("1. Ayuda para buscar llaves perdidas\n");
        printf("2. Probar el sonido de la alarma\n");
        printf("3. Salir\n");
        printf("Elige una opcion (1, 2 o 3): ");
        scanf("%d", &opcion);

        if (opcion == 1) {
            printf("\nVamos a buscar tus llaves...\n");


            if (preguntar("el bano"))       { printf("¡Perfecto! Ve a echar un vistazo al bano.\n"); }
            else if (preguntar("la sala"))  { printf("¡De acuerdo! Ve a revisar la sala.\n"); }
            else if (preguntar("el dormitorio 1")) { printf("¡Bien! Busca en el dormitorio 1.\n"); }
            else if (preguntar("el dormitorio 2")) { printf("¡Entendido! Revisa el dormitorio 2.\n"); }
            else if (preguntar("el dormitorio 3")) { printf("¡Vale! Echa una mirada en el dormitorio 3.\n"); }
            else if (preguntar("la cocina"))       { printf("¡Genial! Seguro estan en la cocina.\n"); }
            else if (preguntar("el patio"))        { printf("¡Excelente! Sal a revisar al patio.\n"); }
            else if (preguntar("el garaje"))       { printf("¡Muy bien! Toca buscar en el garaje.\n"); }
            else {
                printf("\nVaya, no parece estar en los lugares de siempre. ¡Sigue buscando!\n");
            }


            do {
                printf("\n¿Quieres volver al menu de inicio? (S/N): ");
                scanf(" %c", &volver);
                if (volver != 's' && volver != 'S' && volver != 'n' && volver != 'N') {
                    printf("  -> Por favor, usa solo 'S' para Si o 'N' para No.\n");
                }
            } while (volver != 's' && volver != 'S' && volver != 'n' && volver != 'N');

            if (volver == 'n' || volver == 'N') {
                opcion = 3;
                printf("\nCerrando todo... ¡Nos vemos pronto!\n");
            }

        }
        else if (opcion == 2) {

            printf("\nIniciando la alarma...\n");
            printf("Escucharas unos sonidos ahora mismo...\n");

            for (int i = 0; i < 5; i++) {
                Beep(1200, 300);
                Sleep(100);
                Beep(800, 300);
                Sleep(100);
            }
            printf("Listo, la alarma termino. Te devuelvo al menu.\n");
        }
        else if (opcion == 3) {
            printf("\n¡Que tengas un excelente dia! Adios.\n");
        }
        else {
            printf("\nEse numero no es valido. Por favor, escribe 1, 2 o 3.\n");
        }

    } while (opcion != 3);

    return 0;
}
